# vimember Design System

> 本文件记录 vimember 的设计系统规则。当前还没有从 Figma 中提取具体颜色、字体、间距和组件规格，因此本文件只定义维护方法和不可猜测边界。

## 1. 设计来源

正式 UI 的唯一视觉来源是用户提供的 Figma 文件中用户指定读取的内容：

https://www.figma.com/design/z9ewZ9itwDRhTMK94G08u1/vimember?node-id=0-1&t=xXw4CqlJl4XHgkaf-1

任何实现前必须先读取用户指定的 Figma 页面、Frame、组件或节点。

## 2. 当前状态

已由用户指定并部分读取确认：

1. `首页` Frame 是首页正式 UI 来源。
2. `首页-切换` Frame 是首页切换设置卡片正式 UI 来源。
3. 首页 Frame 中包含上下超出屏幕的内容和屏幕范围标识，实现时必须区分可视屏幕和可滚动内容。
4. 首页视觉要适配不同尺寸 iPhone。
5. 首页视频展示参考用户准备的“文稿/视频”demo 效果，demo 路径为 `/Users/zhangsiyuan/Documents/视频`。
6. demo 只参考效果，不参考实现方式；即使 demo 与 Figma 有差异，也以 Figma 画出来的感觉为准。

### 2.1 首页屏幕换算规则

首页 `首页` Frame 中的 `iPhone Air` 图层是屏幕范围参考，尺寸为 `420pt * 912pt`。实现首页悬浮控件和屏幕边距时，所有值均按 iOS point 计算，并使用真实设备屏幕尺寸进行缩放。

换算规则：

1. 真实屏幕尺寸优先取 `UIScreen.main.bounds.size`。
2. 横向尺寸、横向位置、左右边距按 `screenWidth / 420` 缩放。
3. 纵向位置、上下边距按 `screenHeight / 912` 缩放。
4. 不得使用滚动内容高度或普通容器高度替代真实屏幕高度来计算悬浮控件距屏幕边缘的位置。

当前首页悬浮控件基准值：

| 组件 | Figma 基准尺寸 | Figma 基准位置/边距 | 响应规则 |
| --- | --- | --- | --- |
| 右上切换按钮 | `44pt * 44pt` | 距右 `20pt`，距上 `69pt` | 以 `420pt * 912pt` 为参考，按真实屏幕 pt 缩放 |
| 底部搜索框 | `306pt * 50pt` | 底部整体距左 `27pt`，距底 `27pt` | 宽度按横向缩放，高度和底边距按已确认规则缩放 |
| 右下添加按钮 | `50pt * 50pt` | 距搜索框 `10pt`，底部整体距右 `27pt`，距底 `27pt` | 以底部整体为一组按真实屏幕 pt 定位 |

尚未提取具体数值：

1. 颜色 Token。
2. 字体名称与字号体系。
3. 间距体系。
4. 圆角体系。
5. 阴影体系。
6. 图标体系。
7. 首页视图组件规格。
8. 画廊视图组件规格。
9. 详情视图组件规格。
10. 导入和编辑流程组件规格。
11. `首页` Frame 中视频、纯色底和文字区的精确尺寸。
12. `首页-切换` Frame 中卡片、选项、状态和动效规格。

以上内容不得自行猜测。

## 3. 设计系统记录格式

读取 Figma 并确认后，按以下结构补充：

### 3.1 Color Tokens

| Token | Light | Dark | 用途 | 来源 Figma 节点 |
| --- | --- | --- | --- | --- |

### 3.2 Typography

| Token | Font | Size | Weight | Line Height | 用途 | 来源 Figma 节点 |
| --- | --- | --- | --- | --- | --- | --- |

### 3.3 Spacing

| Token | Value | 用途 | 来源 Figma 节点 |
| --- | --- | --- | --- |

### 3.4 Radius

| Token | Value | 用途 | 来源 Figma 节点 |
| --- | --- | --- | --- |

### 3.5 Components

每个组件必须记录：

1. 组件名称。
2. 使用场景。
3. Figma 来源节点。
4. 尺寸与响应规则。
5. 状态：默认、按下、选中、加载、空、错误。
6. 是否有动效。

## 4. 视频相关视觉规则

已明确的产品行为：

1. 首页视频和文字摘要必须放在一起。
2. 画廊只展示摘要。
3. 详情页展示完整视频和完整文字。
4. 详情页视频循环自动播放。
5. 首页滚动停止后，只播放当前屏幕中最主要的一条可见视频。
6. 导入视频后必须进入编辑页。
7. 详情页支持再次编辑和删除，但入口位置与视觉样式由 Figma 指定。
8. 删除不影响系统相册原视频。
9. 首页每个视频下方有一块纯色底，纯色底高度大于视频高度并向下超出视频下边界。
10. 首页视频下方区域需要渐变模糊和透明度处理，使视频与纯色底融合。
11. 纯色底颜色需要实时取自视频模糊后区域的颜色。
12. 首页文字区域结构为标题、日期、横线和正文。
13. 首页正文超过两行时只显示两行。
14. 首页左下角跑道圆为搜索框入口。
15. 首页右下角圆形按钮为添加新内容入口。
16. 首页右上角圆形按钮打开切换设置。
17. 切换设置卡片按 `首页-切换` Frame 制作，包含日期显示方式切换和切换到画廊视图。

待确认的视觉细节：

1. 视频比例。
2. 视频圆角。
3. 封面图截取方式。
4. 播放中是否显示控制条。
5. 摘要字数和行数。
6. 画廊卡片比例和列数。
7. 详情页文字排版。
8. 再次编辑按钮样式和位置。
9. 删除入口、确认流程和视觉样式。
10. 首页搜索框展开后的状态和结果页视觉。
11. 日期显示方式切换的选项状态、规则和文案。
12. 纯色底动态取色的算法细节、采样区域和失败兜底颜色。
13. 不同 iPhone 尺寸下首页内容的缩放、裁切和安全区规则。

## 5. 维护规则

1. 不得为了方便实现而改变 Figma 视觉意图。
2. 不得从旧项目参考文件继承颜色、字体或布局。
3. 新增 Token 必须写入本文件。
4. 修改 Token 必须说明原因和影响范围。
5. 如果 Figma 与代码实现能力存在冲突，先向用户说明，再等待确认。
