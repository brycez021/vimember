# vimember Agent Guide

> 本文件定义 vimember 的产品方向、协作规则和不可随意改动的行为边界，面向 Codex / Claude / Cursor 等 AI 编程工具。
> 若与 `contributing_ai.md` 或其他文档冲突，以本文件为准。

## 1. 项目定位

**产品名：** vimember

**一句话定位：** 一个 iOS 视频日记 App，用户从系统相册导入视频，为视频配上一段文字，然后用首页列表、画廊和详情页回看这些视频记忆。

**当前阶段：** 已完成基础指导文件、iOS 工程脚手架、首页原型、添加视频流程原型和详情页原型；首页视频卡片、动态取色、文字摘要、悬浮入口、切换设置卡片、横竖屏底色与渐进透明融合、详情页文字层和添加流程正在按 Figma 与用户截图校准。

## 2. 参考资料边界

项目内 `指导文件参考/` 文件夹保存的是另一个 iOS 项目的指导性文件，仅作为文档结构、协作方式、风险意识的参考。

使用参考资料时必须遵守：

1. 不得照搬旧项目的产品语义、页面结构或代码路径。
2. 不得把旧项目的“照片记忆”“回忆/相册”“瀑布流”等规则当成 vimember 的规则。
3. vimember 的实际规则以本文件、用户最新要求和用户指定读取的 Figma 内容为准。
4. 如果发现旧参考文件与 vimember 要求冲突，必须忽略旧参考文件。

## 3. 核心产品要求

### 3.1 视频导入与编辑

1. 用户可以从 iOS 系统相册导入视频。
2. 用户可以给导入的视频添加文字。
3. 添加文字后完成编辑，形成一条视频日记。
4. 导入视频后必须进入编辑页。
5. 首页右下角加号点击后进入视频选择页面。
6. 视频选择页面视觉来源为 Figma `选择视频` Frame。
7. 视频选择页面中所有视频不管原始横竖比例，都裁剪成正方形展示。
8. 点击某个视频后显示 Figma 中绘制的高亮标记和小对号。
9. 视频选择页左上角返回首页；选中视频后点击右上角对号进入下一步。
10. 选中视频后点击右上角对号，必须先进入视频预览页，而不是先进入添加文字页。
11. 视频预览页视觉来源为 Figma `添加-视频页` Frame。
12. 视频预览页只展示刚才选中的一条视频，视频自动播放。
13. 视频预览页不管横屏还是竖屏，视频都居中显示，并在视频上边缘和下边缘使用与横屏详情页相同思路的丝滑融合效果融入动态取色背景。
14. 视频预览页背景颜色逻辑和首页视频背景颜色逻辑一致：动态取色视频底色，视频边缘与背景融合。
15. 视频预览页底部有向下箭头；点击后页面向下滚动到添加文字页。
16. 添加文字页视觉来源为 Figma `添加-文字页` Frame。
17. 添加文字页左上角返回视频选择页；右上角确定完成加视频流程并回到首页。
18. 添加文字页上方有向上箭头；点击后页面向上滚动回视频预览页。
19. 添加文字页背景颜色必须与视频预览页下方纯色背景一致，使用选中视频的动态取色底色。
20. 添加文字页正文不设固定显示高度，内容超出屏幕后通过页面滚动查看。
21. 支持后续再次编辑文字；当前已实现从详情页编辑按钮进入添加文字/视频编辑态。
22. 当前需求只描述“视频 + 文字”的配对，不包含滤镜、剪辑、变速、配乐、贴纸等视频编辑能力，除非用户后续明确要求。

### 3.2 首页视图

1. 首页展示所有已加入的视频日记。
2. 展示方式为上下排列。
3. 每条内容必须把视频和它搭配的文字放在一起。
4. 首页只展示文字摘要，不展示全文。
5. 点击首页中的内容可以进入详情视图。
6. 用户上下滑动首页时，视频不动；一旦用户停止滑动，当前屏幕上最主要的一条可见视频需要立刻开始播放。
7. 首页同一时间只播放最主要的一条视频，不同时播放多条视频。
8. Figma 中名为 `首页` 的 Frame 是首页正式 UI 的来源。该 Frame 内存在上下超出屏幕范围的内容，并包含屏幕范围标识；实现时必须理解屏幕范围与滚动内容的关系。
9. 首页必须适配不同尺寸的 iPhone，不得只按某一个 Figma 画布尺寸硬编码布局。
10. 首页视频展示方式参考用户准备的 demo 中“文稿/视频”效果，但只参考效果，不参考 demo 的实现方式；正式制作以 Figma 中 `首页` Frame 的感觉为准。
11. 每个视频需要有一块取色视频纯色底。竖屏状态纯色底与视频同高，不向下超出视频高度；横屏状态纯色底高度大于视频高度，并向下超出视频下边界。
12. 视频下方部分需要叠加从上到下逐渐增强的模糊和逐渐增强的透明度，让视频与纯色底自然融合，不能出现清晰视频、模糊层或纯色底之间的明显分界线。
13. 纯色底颜色需要实时取自视频底部融合参考区域的颜色。
14. 首页文字区域包含标题、日期、横线和正文。
15. 首页正文最多显示两行；超过两行时只显示两行，全文仍在详情页展示。
16. 首页左下角的跑道圆是搜索框入口。
17. 首页右下角的圆形按钮用于添加新内容。
18. 首页右上角的圆形按钮用于打开切换设置。
19. 切换设置卡片的视觉样式以 Figma 中 `首页-切换` Frame 为准，包含日期显示方式切换和切换到画廊视图。
20. 日期显示方式切换规则用户后续再提供，当前不得自行实现具体规则。
21. 首页视频按时间倒序排列，越新的视频越靠上；刚打开首页时显示最新视频，最上面视频卡片的上边界必须与屏幕上边界对齐。

### 3.3 画廊视图

1. 画廊视图展示已经加入的视频日记。
2. 画廊视图只展示文字摘要，不展示全文。
3. 点击画廊中的内容可以进入详情视图。
4. 画廊的具体布局、尺寸、列数、封面截帧方式和交互动效必须以后续 Figma 指定内容为准，不得猜测。

### 3.4 详情视图

1. 详情视图展示完整视频和完整文字。
2. 详情视图中的视频需要自动播放。
3. 详情视图中的视频需要反复循环播放。
4. 详情视图左上角为返回按钮；右上角圆形按钮为删除，右上跑道圆为编辑。
5. 点击删除必须先弹出确认，再彻底删除。
6. 删除只删除 App 本地数据库记录和 App 沙盒里的视频文件，不删除系统相册原视频。
7. 点击编辑进入添加文字/视频编辑态，编辑页面继续作为全屏浮层显示。
8. 详情页文字卡片初始覆盖在视频底部，视频位置保持不变；向上滑动文字卡片时，文字页滑上来占满全屏，再继续向下滚动查看全文。
9. 在屏幕靠上位置明显向下滑动时，文字页收回到原本位置。
10. 详情页文字展开后的背景颜色必须沿用视频底部动态取色底色。
11. 详情页横屏视频在屏幕中视觉居中，视频上边缘和下边缘都需要丝滑融入动态取色背景。

## 4. 已确认的产品与技术决策

1. 视频导入后复制到 App 本地沙盒，App 展示和播放使用自己的本地视频文件。
2. App 可以保存原相册视频的来源标识作为记录或未来关联，但不能依赖系统相册原视频作为唯一播放来源。
3. 文字保存在 App 本地数据库。
4. 摘要规则为展示正文前面固定字数的一部分；具体字数、行数和截断样式后续由用户确认。
5. 首页停止滚动后只播放当前屏幕中最主要的一条视频；“最主要”的判定细节在实现前结合 Figma 和用户确认。
6. 导入视频后必须进入编辑页。
7. 支持再次编辑；当前入口为详情页右上跑道圆编辑按钮。
8. 支持删除视频日记；删除只删除 App 本地数据库记录和 App 沙盒里的视频文件，任何时候都不删除系统相册原视频。
9. iCloud、账号、同步、备份、导出等能力后续再说，MVP 不提前实现。
10. 最低支持版本定为 iOS 17.0。
11. 第二版可能加入视频分类功能；第一版不实现分类 UI 和分类流程，但数据结构设计可以避免明显阻碍后续扩展。
12. 项目使用 GitHub 做版本管理，后续需要创建 GitHub 仓库、推送代码并做好分支管理。

## 5. Figma 使用规则

用户提供的 Figma 文件是正式 UI 实现的视觉源：

https://www.figma.com/design/z9ewZ9itwDRhTMK94G08u1/vimember?node-id=0-1&t=xXw4CqlJl4XHgkaf-1

正式制作 UI 时必须遵守：

1. 必须按用户当次指令读取用户指定的 Figma 页面、Frame、组件或节点。
2. 不得在未读取指定 Figma 内容前凭想象实现界面。
3. 不得把当前文档中的文字描述替代 Figma 视觉细节。
4. 颜色、字体、间距、圆角、阴影、图标、布局比例、状态样式和文案层级都应尽量贴近 Figma。
5. 如果 Figma 中存在不清楚、缺失或互相冲突的内容，必须先询问用户。

### 5.1 首页屏幕尺寸换算规则

首页 `首页` Frame 中的手机屏幕范围以 Figma 的 `iPhone Air` 图层为参考，尺寸为 `420pt * 912pt`。实现首页悬浮按钮、屏幕边距和与屏幕边缘有关的位置时，必须遵守：

1. 所有屏幕相关尺寸和位置按真实设备屏幕尺寸计算，单位使用 iOS point，不使用像素。
2. 真实屏幕尺寸优先取 `UIScreen.main.bounds.size` 作为换算依据。
3. 以 Figma `iPhone Air` 的 `420pt * 912pt` 为参考尺寸：横向尺寸和左右边距按 `screenWidth / 420` 缩放，纵向位置和上下边距按 `screenHeight / 912` 缩放。
4. 不得只用 SwiftUI 容器高度、滚动内容高度或安全区 inset 代替真实屏幕高度来计算首页悬浮按钮位置。
5. 当前首页悬浮控件已确认的基准值为：右上切换按钮 `44pt * 44pt`，距右 `20pt`、距上 `52pt`；底部搜索框 `306pt * 50pt`，右下添加按钮 `50pt * 50pt`，两者间距 `10pt`，底部整体左右边距 `27pt`，距真实屏幕底部 `27pt`。
6. 首页右上切换按钮的尺寸和上/右边距必须与详情页圆形按钮保持一致。

### 5.2 首页视频底色与渐进融合规则

已从 Figma `首页` Frame 读取并与用户确认的规则：

1. 首页 `首页` Frame 节点为 `13:877`。
2. 竖屏示例 `image 59` 尺寸为 `420pt * 745pt`，竖屏纯色底与视频同高，不得超出视频高度。
3. 竖屏文字标题顶部位于视频底部上方约 `114pt`；实现时文字不能与视频清晰内容硬碰，文字背后需要有从下到上逐渐减弱的模糊融合。
4. 横屏示例中纯色底节点 `Rectangle 1032` 为 `420pt * 322pt`，横屏视频节点 `image 58` 为 `420pt * 236pt`，纯色底向下超出视频 `86pt`。
5. 横屏文字标题顶部位于视频底部上方约 `28pt`。
6. Figma 横屏图片层使用 `PROGRESSIVE` layer blur，最大半径为 `100`，模糊从视频下半段开始向下逐渐增强；当前 SwiftUI 原型不新增横屏封面/视频模糊层，而是用清晰视频渐隐 mask 和动态取色透明渐变罩近似“边缘化开”的效果。
7. 横屏实现必须让视频在到达真实下边界前已经自然化开，再由下方纯色底接住，避免视频真实底边或色块顶部出现明显横向分界线。
8. 竖屏实现使用更大范围的取色渐变罩和轻微视频底部渐隐来加强文字背后的融合；竖屏纯色底仍与视频同高。
9. 当前首页文字层必须绘制在视频融合层之上；纯色渐变罩不得盖在文字上方。
10. 当前首页竖屏文字已按用户截图微调，横屏卡片文字逻辑已按用户要求复制竖屏当前逻辑：标题、日期、横线、正文的 top-from-bottom 基准分别为 `125pt`、`91.5pt`、`66pt`、`60.5pt`，整体偏移为 `0`，左边距为 `23pt`。
11. 首页最上方和最下方叠加轻量白色渐变模糊层，用于柔和屏幕边缘；该层应保持淡白透明，主要依赖渐变模糊，不得明显遮挡内容。

## 6. 不明确事项处理规则

用户已经明确要求：任何细节不懂都不要猜，而是向用户询问。

因此 AI 在开发 vimember 时必须：

1. 遇到产品行为不明确，先问用户。
2. 遇到 Figma 未指定的视觉细节，先问用户。
3. 遇到未确认的权限、播放策略、摘要规则等会影响长期架构的选择，先列出问题并等待确认。
4. 不得用“通用 App 常见做法”自动填补关键需求。
5. 可以在文档中标注“待确认”，但不得把待确认事项当成已确定规则实现。

## 7. 当前待确认问题

以下问题在对应功能正式实现或固化前需要用户确认：

1. 摘要固定字数、行数和截断样式。
2. 首页“最主要的一条视频”的视觉和算法判定方式。
3. 画廊视图的布局：单列、多列、网格、横向、按日期分组等，由 Figma 指定。
4. 首页、画廊、导入和编辑页面后续新增状态的具体 Figma 节点。
5. 视频是否静音自动播放，以及是否显示播放控制条。
6. 是否需要浅色和深色模式。
7. 首页日期显示方式切换的具体规则。
8. 搜索框入口的搜索范围、搜索结果页和空状态。

## 8. 技术方向约束

在用户未提出其他要求前，vimember 应按 iOS 原生 App 的方向规划：

1. 使用 Swift / SwiftUI 作为默认实现方向。
2. 视频导入必须使用 iOS 公开 API，不能依赖私有接口。
3. 视频播放必须使用 iOS 公开的播放能力，并明确管理播放生命周期。
4. 最低支持 iOS 17.0。
5. 本地数据库优先使用 SwiftData；如实际工程条件不适配，必须说明原因再改用 Core Data 或 SQLite。
6. 视频文件复制到 App 沙盒后由 App 管理，数据库记录保存本地视频路径、文字、摘要相关数据和原相册来源标识。
7. 所有 UI 更新必须在主线程安全范围内完成。
8. 视频缩略图生成、文件访问、数据库读写等重任务不得阻塞主线程。

## 9. GitHub 与分支管理

1. 项目使用 GitHub 做版本管理。
2. `main` 分支用于稳定版本。
3. 开发工作默认使用 `codex/` 前缀创建功能分支。
4. 功能完成后通过 PR 合并到 `main`。
5. 未经用户确认，不直接把未验证代码推到 `main`。
6. 后续创建 GitHub 仓库、首次推送和 PR 流程需要按用户明确指令执行。

## 10. 核心行为不可破坏

以下行为属于 vimember 的核心体验：

1. 视频日记必须由“视频 + 文字”组成。
2. 首页和画廊都只展示摘要。
3. 详情页必须展示全文。
4. 首页滚动时视频不播放或不运动；滚动停止后，只播放最主要的一条可见视频。
5. 详情页视频自动循环播放。
6. 正式 UI 必须以用户指定读取的 Figma 内容为准。
7. 删除视频日记时，任何时候都不能删除系统相册原视频。
8. 首页 `首页` Frame 和 `首页-切换` Frame 的视觉意图不得被通用模板替代。
9. 首页视频纯色底、横竖屏差异化底色高度、渐进透明融合、动态取色视频底色、标题/日期/横线/两行正文结构属于首页已确认体验。

## 11. AI 工作规则

AI 修改 vimember 时必须：

1. 每次只处理用户指定的任务。
2. 不在用户只要求计划或文档时开始制作 App 功能。
3. 不做大规模无确认重写。
4. 修改前先确认当前仓库状态和相关文件。
5. 修改后说明新增或修改了哪些文件。
6. 无法验证的内容必须标注 `NOT VERIFIED`。
7. 涉及架构选择或用户体验分歧时先询问用户。

## 12. 每次会话建议阅读顺序

1. `AGENTS.md`
2. `contributing_ai.md`
3. `README.md`
4. `ROADMAP.md`
5. `Designsystem.md`
6. `doc/ARCH_REQUIREMENTS.md`
7. `doc/ARCH_TEST_REVIEW.md`
