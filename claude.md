# vimember 项目全景文档

> 本文档用于帮助 AI 快速理解 vimember。它不是产品最终规格，遇到冲突时以 `AGENTS.md`、用户最新指令和用户指定读取的 Figma 内容为准。

## 1. 项目概述

vimember 是一个计划中的 iOS 视频日记 App。

核心流程：

1. 用户从系统相册导入视频。
2. App 将视频复制到本地沙盒。
3. 用户进入编辑页并为视频添加文字。
4. 用户结束编辑，保存为一条视频日记。
5. 用户可以在首页列表或画廊视图浏览视频日记摘要。
6. 用户可以点进详情页查看完整视频和完整文字。

当前阶段已完成文档整理、iOS 工程脚手架、首页原型、添加视频流程原型和详情页原型，正在按 Figma 与用户截图校准首页视频卡片、动态取色、横竖屏底色、渐进透明融合、详情页文字层和添加/编辑流程。

## 2. 已明确需求

### 2.1 视频日记

每条日记由一个视频和一段文字组成。

已确认：

1. 视频文件复制到 App 沙盒，由 App 使用本地文件播放和展示。
2. 文字保存到 App 本地数据库。
3. 可以保存原相册视频来源标识，但不能依赖它作为唯一播放来源。
4. 最低支持 iOS 17.0。

### 2.2 首页视图

首页展示所有视频日记，上下排列。视频和搭配文字放在一起，只显示文字摘要。点击可进入详情页。首页按时间倒序展示，最新视频在最上面；刚打开首页时，最上面视频卡片上边界与屏幕上边界对齐。

首页滚动行为：

1. 上下滑动时视频不动。
2. 停止滑动后，当前屏幕中最主要的一条可见视频立刻开始播放。
3. 同一时间只播放一条视频。

首页视觉和入口：

1. Figma `首页` Frame 是首页正式 UI 来源，Frame 内包含上下超出屏幕内容和屏幕范围标识。
2. 首页必须适配不同尺寸 iPhone。
3. 视频展示方式参考 `/Users/zhangsiyuan/Documents/视频` 中用户准备的“文稿/视频”demo，但只参考效果，不参考实现方式；最终以 Figma 的整体感觉为准。
4. 每个视频有动态取色纯色底；竖屏纯色底与视频同高，横屏纯色底高于视频并向下超出视频下边界。
5. 视频下方部分使用从上到下逐渐增强的动态取色渐变和透明度，与纯色底融合，不能出现明显横向分界线；横屏当前通过清晰视频边缘渐隐和窄范围透明取色罩实现“边缘化开”。
6. 纯色底颜色实时取自视频底部融合参考区域。
7. 文字区域包含标题、日期、横线和正文；正文超过两行时首页只显示两行。
8. 左下角跑道圆为搜索框入口。
9. 右下角圆形按钮为添加新内容入口。
10. 右上角圆形按钮打开切换设置。
11. 切换设置卡片按 Figma `首页-切换` Frame 制作，包含日期显示方式切换和切换到画廊视图。
12. 日期显示方式切换规则待用户后续提供。
13. 右上角按钮与详情页圆形按钮保持同尺寸、同上/右边距。
14. 首页上下屏幕边缘有淡白色渐变模糊层。
15. 当前横屏卡片文字逻辑已按用户要求复制竖屏当前文字逻辑。

### 2.2.1 添加视频流程

当前已确认并实现的新增流程：

1. 首页加号进入 `选择视频` 页面。
2. 选中视频后点右上角对号，先进入 `添加-视频页`。
3. `添加-视频页` 中横屏和竖屏视频都在屏幕中居中显示，上下边缘丝滑融入动态取色背景。
4. `添加-视频页` 底部向下箭头进入 `添加-文字页`。
5. `添加-文字页` 背景颜色与视频页下方纯色背景一致，正文可随页面滚动显示长文。
6. `添加-文字页` 上方箭头滚回视频页，右上角对号保存并回首页。

### 2.3 画廊视图

画廊展示所有视频日记，只显示文字摘要。点击可进入详情页。

画廊具体视觉形式待 Figma 指定。

### 2.4 详情视图

详情页展示完整视频和完整文字。视频自动播放并循环播放。详情页当前已实现左上返回、右上删除、右上编辑；删除会先确认，只删除本地记录和 App 沙盒视频。文字层可从底部上滑展开为完整文字页，展开背景沿用视频底部动态取色底色。横屏详情视频在屏幕中视觉居中，并在上下边缘融入同色背景。

### 2.5 删除规则

删除只删除 App 本地数据库记录和 App 沙盒中的视频文件，任何时候不删除系统相册原视频。

### 2.6 未来版本

第二版可能加入视频分类功能。第一版不实现分类 UI 和分类流程，但数据结构设计应避免明显阻碍后续扩展。

### 2.7 GitHub 版本管理

项目使用 GitHub 做版本管理。`main` 分支用于稳定版本，开发工作默认使用 `codex/` 前缀功能分支，完成后通过 PR 合并。

### 2.8 Figma 优先

正式制作时，必须根据用户指令读取这个 Figma 文件中的指定内容：

https://www.figma.com/design/z9ewZ9itwDRhTMK94G08u1/vimember?node-id=0-1&t=xXw4CqlJl4XHgkaf-1

实现效果要尽量接近 Figma。

## 3. 与旧参考项目的关系

`指导文件参考/` 中的文件来自另一个项目。它们可以参考的部分是：

1. 文档组织方式。
2. AI 协作规则写法。
3. 架构边界意识。
4. 测试和性能风险意识。

不能继承的部分是：

1. 旧项目产品名称。
2. 旧项目页面结构。
3. 旧项目照片语义。
4. 旧项目颜色、字体、卡片和瀑布流规则。
5. 旧项目代码路径。

## 4. 建议技术分层

以下是后续可能采用的分层方向，正式实现前仍需结合用户确认和实际工程状态调整。

### App

应用入口、环境配置、权限描述和根导航。

### Features

按功能分区：

1. Import：相册视频导入。
2. Editor：视频文字编辑。
3. Home：纵向首页列表。
4. Gallery：画廊。
5. Detail：详情页。

### Domain

视频日记数据模型、摘要规则、播放状态规则。

### Services

相册访问、视频资源管理、缩略图生成、持久化、权限。

### DesignSystem

来自 Figma 的颜色、字体、间距、组件和图标。

## 5. 关键风险

1. 本地复制视频会占用手机存储，需要后续设计容量提示和清理策略。
2. 首页视频自动播放会影响滚动性能和电量。
3. “最主要的一条视频”的判定需要结合 Figma 和用户确认。
4. Figma 未读取前实现 UI 容易偏离用户设计。
5. 摘要字数和截断样式未定，会影响首页和画廊的信息密度。
6. 详情页循环播放需要正确释放播放器资源。
7. 首页视频下方动态取色、渐变模糊和透明度效果需要性能验证，不能阻塞滚动。
8. 首页参考 demo 路径已确认为 `/Users/zhangsiyuan/Documents/视频`；demo 只是效果示意，正式实现仍要尽量接近 Figma。

## 6. 待确认问题

1. 摘要固定字数、行数和截断样式。
2. 首页“最主要的一条视频”的具体判定方式。
3. 画廊布局。
4. 视频是否静音自动播放，以及是否显示播放控制条。
5. 是否需要浅色和深色模式。
6. 搜索、分享、导出、账号、同步、备份等后续能力是否进入未来版本。
7. 首页日期显示方式切换规则。
8. 首页搜索框的搜索范围和结果页样式。

## 7. 当前文档地图

1. `AGENTS.md`：最高优先级项目规则。
2. `contributing_ai.md`：AI 协作流程。
3. `README.md`：项目说明。
4. `ROADMAP.md`：阶段计划。
5. `Designsystem.md`：设计系统记录规则。
6. `doc/ARCH_REQUIREMENTS.md`：架构要求。
7. `doc/ARCH_TEST_REVIEW.md`：测试和审查基线。
